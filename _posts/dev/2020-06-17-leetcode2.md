---
title: "leetcode 2"
categories: [Development]
---

<!-- prettier-ignore -->
* Do not remove this line (it will not be displayed)
{:toc}

# [1-两数之和](https://leetcode.com/problems/two-sum/)

给定一个整数数组 nums 和一个目标值 target，在数组中找出和为 target 的两个整数，并返回他们的索引。

用哈希表存储 {key: nums[i], value: i}，看要找的数是否在哈希表中。

```py
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hashMap = {}
        for i in range(len(nums)):
            rem = target - nums[i]
            if rem in hashMap:
                return [hashMap[rem], i]
            hashMap[nums[i]] = i
        return []
```

# [15-三数之和](https://leetcode.com/problems/3sum/)

判断 nums 中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出所有满足条件且不重复的三元组。

N 个数的数组，穷举法需要枚举所有的组合数：`N * (N-1) * (N-2)`，时间复杂度 O(n^3)，要想优化它，可以先对数组进行排序。

排序后的数组，我们发现，当第一重循环 a 固定时，b 和 c 形成了一种“相互牵制”的关系，b 增大，c 就要减小，因此，b、c 可以分别从数组的两端向中间遍历，也就是“双指针”。

```py
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums.sort()
        res = []
        N = len(nums)
        for i in range(N-2):
            if i > 0 and nums[i] == nums[i-1]:
                continue
            lo = i + 1
            hi = N - 1
            while lo < hi:
                S = nums[i] + nums[lo] + nums[hi]
                if S == 0:
                    res.append([nums[i], nums[lo], nums[hi]])
                    lo += 1
                    hi -= 1
                    while lo < hi and nums[lo] == nums[lo-1]:
                        lo += 1
                    while lo < hi and nums[hi] == nums[hi+1]:
                        hi -= 1
                elif S < 0:
                    lo += 1
                else:
                    hi -= 1
        return res
```

# 链表

## [2-两数相加](https://leetcode.com/problems/add-two-numbers/)

考察链表基本的遍历操作，头部指针的存储（dummy），进位的技巧。

```py
class Solution:
    def addTwoNumbers(self, l1: ListNode, l2: ListNode) -> ListNode:
        dummy = cur = ListNode(0)
        carry = 0 # 存储进位
        while l1 or l2 or carry:
            if l1:
                carry += l1.val
                l1 = l1.next
            if l2:
                carry += l2.val
                l2 = l2.next
            cur.next = ListNode(carry % 10) # 取余数
            cur = cur.next
            carry = 1 if carry >= 10 else 0
        return dummy.next
```

## [21-合并两个有序链表](https://leetcode.com/problems/merge-two-sorted-lists/)

链表基本操作：

```py
class Solution:
    def mergeTwoLists(self, l1: ListNode, l2: ListNode) -> ListNode:
        dummy = cur = ListNode(0)
        while l1 or l2:
            if l1 and l2 and (l1.val < l2.val) or l1 and not l2:
                cur.next = l1
                cur = cur.next
                l1 = l1.next
            else:
                cur.next = l2
                cur = cur.next
                l2 = l2.next
        return dummy.next
```

递归写法：

```py
class Solution:
    def mergeTwoLists(self, l1: ListNode, l2: ListNode) -> ListNode:
        if l1 == None:
            return l2
        if l2 == None:
            return l1
        if l1.val < l2.val:
            l1.next = self.mergeTwoLists(l1.next, l2)
            return l1
        else:
            l2.next = self.mergeTwoLists(l1, l2.next)
            return l2
```

## [24-两两交换链表节点](https://leetcode.com/problems/swap-nodes-in-pairs/)

链表节点交换的基本操作。

```py
class Solution:
    def swapPairs(self, head: ListNode) -> ListNode:
        if not head or not head.next:
            return head
        tmp = head.next
        head.next = self.swapPairs(tmp.next)
        tmp.next = head
        return tmp
```

## [141-链表中是否有环](https://leetcode.com/problems/linked-list-cycle/)

给定一个链表，判断链表中是否有环。如果链表中有某个节点，可以通过连续跟踪 next 指针再次到达，则链表中存在环。

比较容易想到的是用哈希表或哈希集合存储访问过的节点，当再次访问到时，说明有环，需要 O(N) 的空间。

比较有技巧的是快、慢指针，如果列表不存在环，那么快指针就先到达了终点；反之，快、慢指针会在环中的某个位置相遇。时间复杂度 O(N)，空间复杂度 O(1)。

```py
class Solution:
    def hasCycle(self, head: ListNode) -> bool:
        slow = ListNode(0)
        slow.next = head
        quick = head
        while True:
            if not quick or not quick.next:
                return False
            if slow == quick:
                return True
            slow = slow.next
            quick = quick.next.next
        return True
```

## [206-反转链表](https://leetcode.com/problems/reverse-linked-list/)

迭代版本：

```py
class Solution:
    def reverseList(self, head: ListNode) -> ListNode:
        pre = None
        cur = head
        while(cur != None):
            tmp = cur.next
            cur.next = pre
            pre = cur
            cur = tmp
        return pre
```

递归版本：

```py
class Solution:
    def reverseList(self, head: ListNode) -> ListNode:
        if head == None or head.next == None:
            return head
        tail = self.reverseList(head.next)
        head.next.next = head
        head.next = None
        return tail
```

![img](/assets/images/295A1F6D-4E08-4CB0-8F58-2C011BF5DEFD.jpg)

# 图

## [210-课程表 II](https://leetcode.com/problems/course-schedule-ii/)（拓扑排序）

你这个学期必须选修 n 门课程，记为 0 到 n-1。在选修 5 课程之前需要先修 3 课程，则用 [5, 3] 数组来表示这样的依赖关系。求可以学完课程的顺序。可能会有多个顺序，只需要返回其中一种；如果无法完成学习，返回空数组。

对于有优先级次序约束的调度问题，其计算机模型是有向图的拓扑排序，即将有向图的顶点按照以下的顺序排序：图中所有有向边，从序列中较前的顶点指向序列中较后的顶点。拓扑排序可能没有，也可能不止一种。一个有向图存在拓扑排序当且仅当它是一个有向无环图。

求出一种拓扑排序方法的最优时间复杂度为 O(n+m)，其中 n 和 m 分别是有向图 G 的节点数和边数。

深度优先搜索：当搜索到叶子节点进行回溯的时候，我们把该节点放入栈中。最终从栈顶到栈底的序列就是一种拓扑排序。

```py
class Solution:
    def dfs(self, adjacency, i, visited, res):
        visited[i] = 1
        for v in adjacency[i]:
            if visited[v] == 0:
                if not self.dfs(adjacency, v, visited, res):
                    return False
            elif visited[v] == 1:
                return False
        visited[i] = 2
        res.append(i)
        return True

    def findOrder(self, numCourses: int, prerequisites: List[List[int]]) -> List[int]:
        # 构造邻接表
        adjacency = [[] for _ in range(numCourses)]
        for des, src in prerequisites:
            adjacency[src].append(des)
        # 标记每个节点的状态：0 未搜索，1 搜索中，2 已完成
        visited = [0] * numCourses
        res = []
        for i in range(numCourses):
            if not visited[i]:
                if not self.dfs(adjacency, i, visited, res):
                    return []
        return res[::-1] # 我们用列表模拟栈，最后需要输出栈顶到栈底，需将数组反序输出
```

广度优先搜索：构建邻接表时维护入度信息。在拓扑排序中，最前面的节点入度一定为 0，也就是它没有先修课程要求。当我们将一个节点加入答案中后，它相邻节点的入度均减一，代表少了一门先修课程的要求。如果某个节点入度变为 0，代表它可以开始学习。就这样，不断把入度为 0 的节点加入答案。

```py
class Solution:
    def findOrder(self, numCourses: int, prerequisites: List[List[int]]) -> List[int]:
        adjacency = [[] for _ in range(numCourses)]
        inDegrees = [0] * numCourses
        for (des, src) in prerequisites:
            adjacency[src].append(des) # 构造邻接表
            inDegrees[des] += 1 # 维护入度信息
        res = []
        q = []
        for i in range(numCourses):
            if inDegrees[i] == 0:
                q.append(i) # 入度为 0 的节点先入列
        while len(q) != 0:
            i = q.pop()
            res.append(i) # 从队列中取出一个来加入结果中
            for v in adjacency[i]:
                inDegrees[v] -= 1
                if inDegrees[v] == 0:
                    q.append(v)
        return res if len(res) == numCourses else []
```

## [207-课程表](https://leetcode.com/problems/course-schedule/)（拓扑排序）

你这个学期必须选修 n 门课程，记为 0 到 n-1。在选修某些课程之前需要一些先修课程。用长度为 2 的数组来表示这样的依赖关系：[0, 1]。请你判断是否可能完成所有课程的学习？

```py
class Solution:
    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -> bool:
        adjacency = [[] for _ in range(numCourses)]
        inDegrees = [0] * numCourses
        for (des, src) in prerequisites:
            adjacency[src].append(des) # 构造邻接表
            inDegrees[des] += 1 # 维护入度信息
        res = []
        q = []
        for i in range(numCourses):
            if inDegrees[i] == 0:
                q.append(i) # 入度为 0 的节点先入列
        while len(q) != 0:
            i = q.pop()
            res.append(i) # 从队列中取出一个来加入结果中
            for v in adjacency[i]:
                inDegrees[v] -= 1
                if inDegrees[v] == 0:
                    q.append(v)
        return len(res) == numCourses
```

# 排序

## [912-数组排序](https://leetcode.com/problems/sort-an-array/)

归并排序：原始数组 `[4, 1, 3, 2]`；先排序左半边、再排序右半边，得到 `[1, 4, 2, 3]`；最后合并成 `[1, 2, 3, 4]`。

```py
class Solution:
    def sortArray(self, nums: List[int]) -> List[int]:
        N = len(nums)
        aux = [0] * N # auxiliary 辅助数组，归并排序需要额外 N 的存储空间
        self.sort(nums, aux, 0, N-1)
        return nums

    def sort(self, nums, aux, lo, hi):
        if lo >= hi: return
        mid = lo + (hi-lo)//2
        self.sort(nums, aux, lo, mid)
        self.sort(nums, aux, mid+1, hi)
        self.merge(nums, aux, lo, mid, hi)

    def merge(self, nums, aux, lo, mid, hi):
        # copy to aux
        for i in range(lo, hi+1):
            aux[i] = nums[i]
        # merge back to nums
        i = lo
        j = mid + 1
        for k in range(lo, hi+1):
            if i > mid: # 左半边的数取完了，取右半边的
                nums[k] = aux[j]
                j += 1
            elif j > hi: # 右半边的数取完了，取左半边的
                nums[k] = aux[i]
                i += 1
            elif aux[i] <= aux[j]:
                nums[k] = aux[i]
                i += 1
            else:
                nums[k] = aux[j]
                j += 1
```

快速排序：

```py
class Solution:
    def sortArray(self, nums: [int]) -> [int]:
        return self.sort(nums, 0, len(nums) - 1)

    def sort(self, nums, lo, hi):
        if lo >= hi: return nums
        mid = self.partition(nums, lo, hi) # 找分割点，mid 在它最终应该在的位置上
        self.sort(nums, lo, mid-1)
        self.sort(nums, mid+1, hi)
        return nums

    def partition(self, nums, lo, hi):
        pa = nums[lo] # 选择数组第一个数作为分割值
        i = lo + 1
        j = hi
        while True:
            # 比 pa 小的放左边，比 pa 大的放右边
            while(i <= hi and nums[i] < pa):
                i += 1
            while(j >= lo and nums[j] > pa):
                j -= 1
            if i >= j: break
            nums[i], nums[j] = nums[j], nums[i]
            i += 1
            j -= 1
        nums[lo], nums[j] = nums[j], nums[lo] # pa 在它最终应该在的位置上
        return j
```

快速排序算法在平均状况下有着不错的表现，但是对于基准值的选择十分敏感，最坏情况下的算法复杂度是 O(n^2)。

现实中应用的排序，往往根据数据集的特征，采用多种排序算法的混合。比如 C++ std sort 在数组小于 16 个时，则采用插入排序；如果递归层次过深，会采用桶排序等。

## [977-有序数组的平方](https://leetcode.com/problems/squares-of-a-sorted-array/)

给定非递减数组 A，返回每个数字的平方组成的新数组，要求也按非递减排序。

遇到的阿里面试真题。如果直接乘方然后排序，计算次数是 N + NLogN，复杂度是 O(NLogN)。本题中可以利用有序数组、求平方这两个特性，做一些技巧，降低计算次数。

方案一：找到第一个大于零的数，用两个指针分别向左右遍历。

```py
class Solution:
    def sortedSquares(self, A: List[int]) -> List[int]:
        res = []
        j = 0
        N = len(A)
        while j < N and A[j] < 0:
            j += 1
        i = j - 1
        while i >= 0 or j < N:
            if i < 0:
                res.append(A[j] ** 2) # **2 是 python3 的二次方
                j += 1
            elif j >= N:
                res.append(A[i] ** 2)
                i -= 1
            elif A[i] + A[j] < 0:
                res.append(A[j] ** 2)
                j += 1
            else:
                res.append(A[i] ** 2)
                i -= 1
        return res
```

方案二：原数组具有这样一个特性：两边的平方大，中间的平方小；因此可用两个指针从两头往中间遍历。

```py
class Solution:
    def sortedSquares(self, A: List[int]) -> List[int]:
        N = len(A)
        res = [0] * N
        i = 0
        j = N - 1
        k = N - 1
        while i <= j:
            if abs(A[i]) < abs(A[j]):
                res[k] = A[j] ** 2
                j -= 1
            else:
                res[k] = A[i] ** 2
                i += 1
            k -= 1
        return res
```

## [75-颜色排序](https://leetcode.com/problems/sort-colors/)

这是计算机科学经典的荷兰国旗问题。1, 2, 3 代表红白蓝三色，给定数组 [2,0,2,1,1,0]，排序成 [0,0,1,1,2,2]。

要把数组分成三个颜色，需要两个指针 left 和 right，最终的目的是 0 都在 left 的左边，2 都在 right 的右边。

```py
class Solution:
    def sortColors(self, nums: List[int]) -> None:
        left = 0
        right = len(nums) - 1
        i = 0
        # 遍历一次，保持 left < i < right
        while i <= right:
            if nums[i] == 0:
                nums[left], nums[i] = nums[i], nums[left]
                left += 1 # 把 0 放到 left 的左边，移动 left 指针
                i += 1
            elif nums[i] == 1:
                 # 1 已经在 left 的右边，无需处理
                i += 1
            else:
                nums[i], nums[right] = nums[right], nums[i]
                right -= 1 # 把 2 放到了 right 的右边，移动 right 指针
```

如果扩展到四色呢？有一种遍历两次的方法，第一次遍历先记录每个颜色的个数，第二次遍历将每个颜色放到对应位置即可。

## [215-第 k 大的数](https://leetcode.com/problems/kth-largest-element-in-an-array/)

找到数组中第 k 大的数，最容易想到的，用 O(NlogN) 排序，再用 O(1) 取第 k-1 个元素。

本题要掌握的实际是快速选择算法，它与快速排序一样，都是由计算机科学佳托尼·霍尔发明的。

快速排序中，有一个子过程称为分区，可以在线性时间里将一个列表分为两部分，分别是小于基准和大于等于基准的元素。

与快速排序一样，快速选择算法对于基准值的选择非常敏感，可以在切分函数的一开始，随机交换第一个元素与它后面的任意一个元素的位置。

```py
import random
class Solution:
    def partition(self, nums, lo, hi):
        if lo < hi:
            r = random.randint(lo+1, hi)
            nums[lo], nums[r] = nums[r], nums[lo]
        i = lo + 1
        j = hi
        while True:
            while i < hi and nums[i] <= nums[lo]:
                i += 1 # 找到一个比基准值大的数
            while j > lo and nums[j] > nums[lo]:
                j -= 1 # 找到一个比基准值小的数
            if i >= j: break
            nums[i], nums[j] = nums[j], nums[i]
        nums[lo], nums[j] = nums[j], nums[lo]
        return j # j 左边的数都比 nums[j] 小，右边的都比 nums[j] 大

    def findKthLargest(self, nums: List[int], k: int) -> int:
        N = len(nums)
        lo = 0
        hi = N - 1
        k = N - k # 要找第 2 大的数，则它在长度为 6 的数组中的索引是 6 - 2 = 4
        while lo < hi:
            pa = self.partition(nums, lo, hi)
            if k == pa:
                return nums[k]
            elif k > pa:
                lo = pa + 1
            else:
                hi = pa - 1
        return nums[k]
```

# 回文

回文是正着读、反着读都一样的字符串。

## [125-验证回文串](https://leetcode.com/problems/valid-palindrome/)（双指针或双向队列）

双指针：

```py
class Solution:
    def isPalindrome(self, s: str) -> bool:
        i = 0
        j = len(s) - 1
        while i < j:
            while i < j and not s[i].isalnum():
                i += 1
            while i < j and not s[j].isalnum():
                j -= 1
            if s[i].lower() != s[j].lower():
                return False
            i += 1
            j -= 1
        return True
```

双向队列：

```py
class Solution:
    def isPalindrome(self, s: str) -> bool:
        dq = [] # 双向队列
        for c in s.lower():
            if c.isalnum():
                dq.append(c)
        while len(dq) > 1:
            left = dq.pop(0)
            right = dq.pop()
            if left != right:
                return False
        return True
```

## [5-最长回文子串](https://leetcode.com/problems/longest-palindromic-substring/)（中心扩散法）

给定一个字符串，找到其中最长的回文子串。

对于回文子串问题，中心扩散法是更优的解法。长度为 N 的字符串 s[0...n-1] 一共有 2N-1 个中心扩散点。

```py
class Solution:
    left = 0
    right = 0
    def expand(self, s, i, j):
        while i >= 0 and j < len(s) and s[i] == s[j]:
            i -= 1
            j += 1
        i += 1
        j -= 1
        if j - i > self.right - self.left:
            self.left = i
            self.right = j

    def longestPalindrome(self, s: str) -> str:
        for i in range(len(s)):
            self.expand(s, i, i) # aba
            self.expand(s, i, i+1) # abba
        return s[self.left : self.right+1]
```

一、最优子结构：dp[i][j] 表示 s[i...j] 是否回文子串

二、状态转移方程：dp[i][j] = s[i] == s[j] and dp[i+1][j-1]，注意边界条件是 [i + 1, j - 1] 不构成区间

```py
class Solution:
    def longestPalindrome(self, s: str) -> str:
        N = len(s)
        length = 0
        res = ""
        dp = [[False for i in range(N)] for i in range(N)]
        for i in range(N-1, -1, -1):
            for j in range(i, N):
                if i == j:
                    dp[i][j] = True
                elif s[i] == s[j]:
                    # 边界条件是：[i + 1, j - 1] 不构成区间
                    if (j-1)-(i+1)+1 < 2:
                        dp[i][j] = True
                    else:
                        dp[i][j] = dp[i+1][j-1]
                if dp[i][j] and (j - i + 1) > length:
                    length = j - i + 1
                    res = s[i:j+1]
        return res
```

时间复杂度 O(n^2)，空间复杂度 O(n^2)。

## [647-回文子串](https://leetcode.com/problems/palindromic-substrings/)（中心扩散法）

给定一个字符串，计算这个字符串中有多少个回文子串。

对于回文子串问题，中心扩散法是更优的解法。

```py
class Solution:
    res = 0
    def expand(self, s, i, j):
        while i >= 0 and j < len(s) and s[i] == s[j]:
            i -= 1
            j += 1
            self.res += 1
        i += 1
        j -= 1

    def countSubstrings(self, s: str) -> int:
        for i in range(len(s)):
            self.expand(s, i, i)
            self.expand(s, i, i+1)
        return self.res
```

## [516-最长回文子序列](https://leetcode.com/problems/longest-palindromic-subsequence/)（动态规划）

回文问题要抓住一个基本特征，就是中间向两边扩散。假设有字符串 s，我们如果已知 s[i...j] 的最长回文子序列的长度，那么当 i、j 分别向左右扩散时，我们就可以利用 dp[i][j] 的结果进行扩展，因此本题可以用动态规划求解。

假设字符串为 "abcbd"，长度为 5，我们画一个 5x5 的表格，dp[i][j] 表示 s[i...j] 中的最长回文子序列的长度。很显然，这个表格只有当列数 > 行数时才有意义。

一、最优子结构：dp[i][j] 表示 s[i...j] 中的最长回文子序列的长度。

二、状态转移方程

- 如果 i == j，dp[i][j] = 1
- 如果 s[i] == s[j]，则 dp[i][j] = dp[i+1][j-1] + 2

![image](/assets/images/截屏2020-07-2715.34.55.png)

- 如果 s[i] != s[j]，dp[i][j] = max(dp[i+1][j], dp[i][j-1])

![image](/assets/images/截屏2020-07-2715.53.13.png)

dp[0][n-1] 表示 s[0..<N] 中的最长回文子序列长度，因此题解在表格的右上方，遍历方向是从下至上、从左至右。

```py
class Solution:
    def longestPalindromeSubseq(self, s: str) -> int:
        N = len(s)
        dp = [[0] * N for _ in range(N)]
        for i in range(N-1, -1, -1):
            for j in range(i, N):
                if i == j:
                    dp[i][j] = 1
                elif s[i] == s[j]:
                    dp[i][j] = dp[i+1][j-1] + 2
                else:
                    dp[i][j] = max(dp[i+1][j], dp[i][j-1])
        return dp[0][N-1]
```

# 子串问题（滑动窗口 + 哈希表）

## [3-无重复字符的最长子串](https://leetcode.com/problems/longest-substring-without-repeating-characters/)

给定一个字符串，找出其中不含有重复字符的最长子串的长度。

子串问题的特征是，给定一个字符串，找到符合某种条件的子串。通用的解决办法是滑动窗口 + 哈希表。

需要一个哈希表记录每个字符出现的位置。

![img](/assets/images/9F2C1243-7A78-4E7E-B0E5-BDC06EA88745.png)

i 只能右移，不能左移，当发现了重复字符，i = max(i, 重复字符的位置 + 1)，为什么？因为左移的话，刚才已经排除掉的其它重复字符又会包括进来。

```py
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        i = 0
        j = 0
        hashMap = {}
        res = 0
        while j < len(s):
            if s[j] in hashMap:
                i = max(i, hashMap[s[j]] + 1) # i 只能右移，不能左移
            hashMap[s[j]] = j
            res = max(res, j - i + 1)
            j += 1
        return res
```

## [76-包含了指定字符的最小子串](https://leetcode.com/problems/minimum-window-substring/)

给定一个字符串，找出其中包含了指定字符的最小子串。注意：如果 s 中存在这样的子串，我们保证它是唯一答案。

步骤一：不断增加 j 使滑动窗口增大，直到窗口包含了 t 中所有字符。

步骤二：不断增加 i 使滑动窗口缩小，将不必要的元素排除在外，直到碰到一个必须包含的元素，这个时候不能再扔了，再扔就不满足条件了，保存此时滑动窗口的位置。

步骤三：让 j 继续增加，直到碰到 s[j] == s[i]，此时 s[i] 找到了“接班人”，可以被排除在外了，i 继续增加。

需要一个哈希表记录指定的每个字符及其出现次数。计数为正的表示仍然需要的元素数量，计数为负的表示多余的元素数量、计数为 0 表示刚刚好。

例如 need 等于 `{'A':-2,'B':0,'C':1}` 时，A 多余 2 个，B 正好，C 还缺 1 个。

python3：Counter 是 dict 的子类，用于计数可哈希对象。Counter 对象有一个字典接口，如果引用的键没有任何记录，就返回一个 0，而不是弹出一个 KeyError。

```py
class Solution:
    def minWindow(self, s: str, t: str) -> str:
        need = collections.Counter(t) # 创建计数字典
        found = 0
        N = len(s)
        left = 0
        right = N
        i = j = 0
        while j < N:
            if need[s[j]] > 0:
                found += 1
            need[s[j]] -= 1
            if found == len(t) and (right == N or s[j] == s[i]):
                # 找到 t 中的所有字符了，但此时可能有很多其它字符包括了进来
                # 多余的字符，在字典中的计数为负数
                while i < j:
                    if need[s[i]] == 0: # s[i] 必须包括在窗口中，左指针不能再移了
                        break
                    need[s[i]] += 1
                    i += 1
                if j - i < right - left:
                    left, right = i, j
                # 此处不能 break，必须继续遍历 s 直到末尾，找到最优解！
                # 第一次找齐：found == len(t) and right == N
                # 第二次或以后找齐：found == len(t) and s[i] == s[j]，必须找到一个与左指针指向字符相同的字符，左指针才能右移
            j += 1
        if found < len(t): return ""
        return s[left:right+1]
```

# [242-字母异位词](https://leetcode.com/problems/valid-anagram/)

方法一：哈希表计数；方法二：字符串排序后比较。

```py
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        dic1 = collections.Counter(s)
        dic2 = collections.Counter(t)
        return dic1 == dic2
```

# [20-括号匹配](https://leetcode.com/problems/valid-parentheses/)

借助栈数据结构，在 python 可以用列表实现，操作是 `append` 和 `pop`。

```py
class Solution:
    def match(self, c1, c2):
        return c1 == "(" and c2 == ")" or c1 == "{" and c2 == "}" or c1 == "[" and c2 == "]"

    def isValid(self, s: str) -> bool:
        stack = []
        for c in s:
            if c in "([{":
                stack.append(c)
            elif len(stack) == 0:
                return False
            else:
                top = stack.pop()
                if not self.match(top, c):
                    return False
        return len(stack) == 0
```

# [50-快速幂算法](https://leetcode.com/problems/powx-n/)

计算 x 的 n 次幂。

本题的解法被称为「快速幂算法」，有递归和迭代两个版本。

`x ^ n = x ^ 2 ^ (N//2), if n % 2 == 0`

`x ^ n = x * x ^ 2 ^ (N//2), if n % 2 == 1`

递归版本，其思想是分治法：

```py
class Solution:
    def quickPow(self, x, N):
        if N == 0: return 1.0
        y = self.quickPow(x, N // 2)
        return y * y if N % 2 == 0 else y * y * x

    def myPow(self, x: float, n: int) -> float:
        res = self.quickPow(x, abs(n))
        if n < 0:
            res = 1 / res
        return res
```

迭代：

```py
class Solution:
    def myPow(self, x: float, n: int) -> float:
        res = 1
        N = abs(n)
        while N:
            if N % 2 == 1:
                res *= x
            x *= x
            N //= 2
        res = 1 / res if n < 0 else res
        return res
```

# [69-平方根](https://leetcode.com/problems/sqrtx/)

```py
class Solution:
    def mySqrt(self, x: int) -> int:
        if x <= 1: return x
        half = x // 2
        while half * half > x:
            half = half // 2
        while (half+1) ** 2 <= x:
            half += 1
        return half
```

# [149-经过最多点的一条线](https://leetcode.com/problems/max-points-on-a-line/)

给定一个二维平面，平面上有 n 个点，求最多有多少个点在同一条直线上。

知识准备一：最大公约数

## 最大公约数

gcd 函数满足交换律：`gcd(a, b) = gcd(b, a)`；约定 `gcd(a, 0) = a`。

最大公约数的计算可以用素因数分解法、辗转相除法，后者效率更高。

```py
def gcd(a, b): # Greatest Common Divisor
    if b == 0:
        return a
    else:
        return gcd(b, a % b)
```

\*扩展：最小公倍数的求法。

```py
def lcm(a, b): # Least Common Multiple
    return a * b / gcd(a, b)
```

知识准备二：如何判断三点共线？

## 直线方程

一、一般式：`Ax + By + C = 0`（A、B 不同时为 0），适用于所有直线。

二、点斜式：`y - y0 = k(x - x0)`，表示斜率为 k，且经过 (x0, y0) 的直线。适用于不垂直于 x 轴的直线。

三、截距式：`x / a + y / b = 1`，表示与 x 轴、y 轴相交，且 x 轴截距为 a，y 轴截距为 b 的直线。适用于不过原点或不垂直于 x 轴、y 轴的直线。

四、斜截式：`y = kx + b`，表示斜率为 k 且 y 轴截距为 b 的直线。适用于不垂直于 x 轴的直线。

五、两点式：`(y-y1)/(y2-y1)=(x-x1)/(x2-x1), (x1≠x2，y1≠y2)`，表示过 (x1, y1) 和 (x2, y2) 的直线。适用于不垂直于 x 轴、y 轴的直线。

```py
def gcd(a, b):
    if b == 0:
        return a
    else:
        return gcd(b, a % b)

class Solution:
    def maxPoints(self, points: List[List[int]]) -> int:
        n = len(points)
        res = 0
        for i in range(n):
            # 对于每一个点：(xi, yi)
            dic = collections.Counter() # 记录斜率的出现次数
            overlap = 0 # 记录重复的点
            iMax = 0 # 记录经过 i 点的直线，可以经过的最多的点
            for j in range (i+1, n):
                # 与另一个点 (xj, yj) 共同确定一条直线，斜率为 k
                deltaX = points[j][0] - points[i][0]
                deltaY = points[j][1] - points[i][1]
                if deltaX == 0 and deltaY == 0:
                    overlap += 1
                else:
                    # k = deltaY / deltaX
                    # 因为计算机处理浮点数存在精度丢失，我们不能直接记录斜率 k，而是记录斜率的分子分母（约分后）
                    g = gcd(deltaX, deltaY)
                    deltaX /= g
                    deltaY /= g
                    dic[(deltaX, deltaY)] += 1
                    iMax = max(iMax, dic[(deltaX, deltaY)])
            res = max(res, 1 + iMax + overlap)
        return res
```

# 146-LRU Cache

实现本题的两种操作，需要用到一个哈希表和一个双向链表。Java 里面的 `LinkedHashMap` 就可以处理这样的问题。

```java
class LRUCache {
    int capacity;
    LinkedHashMap<Integer, Integer> cache;

    public LRUCache(int capacity) {
        this.capacity = capacity;
        cache = new LinkedHashMap<Integer, Integer>(capacity, 0.75f, true) {
            @Override
            protected boolean removeEldestEntry(Map.Entry eldest) {
                return cache.size() > capacity;
            }
        };
    }

    public int get(int key) {
        return cache.getOrDefault(key, -1);
    }

    public void put(int key, int value) {
        cache.put(key, value);
    }
}
```

本题需要我们自己实现这样的结构。

![image](/assets/images/截屏2020-07-0323.04.38.png)

```python
class LinkedNode:
    def __init__(self, key=0, value=0):
        self.key = key
        self.value = value
        self.prev = None
        self.next = None

class LRUCache:
    def __init__(self, capacity: int):
        self.capacity = capacity
        self.size = 0
        # hash table + double linked list
        self.hashMap = dict()
        self.head = LinkedNode()
        self.tail = LinkedNode()
        # dummy head + dummy tail
        self.head.next = self.tail
        self.tail.next = self.head

    # 添加一个节点到头部
    def addToHead(self, node):
        node.prev = self.head
        node.next = self.head.next
        self.head.next.prev = node
        self.head.next = node

    # 删除一个节点
    def removeNode(self, node):
        node.next.prev = node.prev
        node.prev.next = node.next
        node = None

    # 将一个节点移到头部（标记为最近使用）
    def moveToHead(self, node):
        self.removeNode(node)
        self.addToHead(node)

    # 从尾巴移除一个节点（LRU）
    def removeTail(self):
        node = self.tail.prev
        self.removeNode(node)
        return node

    def get(self, key: int) -> int:
        if key not in self.hashMap:
            return -1
        node = self.hashMap[key]
        # 最近访问过，移动到头部
        self.moveToHead(node)
        return node.value

    def put(self, key: int, value: int) -> None:
        if key not in self.hashMap:
            node = LinkedNode(key, value)
            self.hashMap[key] = node
            self.addToHead(node)
            self.size += 1
            if self.size > self.capacity:
                removed = self.removeTail()
                self.hashMap.pop(removed.key)
                self.size -= 1
        else:
            node = self.hashMap[key]
            node.value = value
            self.moveToHead(node)
```
