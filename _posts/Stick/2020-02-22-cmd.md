---
title: "Command"
categories: [Stick]
---

# 环境

`echo $PATH` 输出：

```
/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin
```

同样的命令，执行程序的顺序是 `/usr/local/bin` > `/usr/bin` > `/bin` ...

# alias

`subl ~/.bash_profile`

# 终端开启自动补全

Type in terminal `vim ~/.inputrc`

Paste the following on separate lines: `set completion-ignore-case on set show-all-if-ambiguous on TAB: menu-complete`

# 使用终端启动 Sublime

使用软连接将 Sublime 提供的命令行工具 subl 连接到`/usr/local/bin/`路径下

```
ln -s /Applications/Sublime\ Text.app/Contents/SharedSupport/bin/subl /usr/local/bin/subl
```

# 快捷键

Page Up: Fn + ⬆️

Page Down: Fn + ⬇️

在 iTerm2 里：Command + Fn + ⬆️

命令行输出过多，可重定向到文件里查看：`sh xx.sh > sh.log` 

# Git

tag 某个 commit：`git tag r20191211_3.4.4 ff5aeaa`

To push a single tag: `git push origin r20191211_3.4.4`

push branch to remote: `git push -u origin branchname`

Delete tag on remote: `git push --delete origin tagname`

# 编辑

| 命令     | 作用                                      |
| -------- | ----------------------------------------- |
| u        | up，上翻半页                              |
| d        | down，下翻半页                            |
| f, fn+⬇️ | 下翻一页                                  |
| b, fn+⬆️ | 上翻一页                                  |
| fn+⬅️    | 行首                                      |
| fn+➡️    | 行尾                                      |
| gg       | 首行                                      |
| G        | 尾行                                      |
| esc+F    | move to the beginning of the next word    |
| esc+B    | move to the beginning of the current word |

# 命令

`ctrl+u`: clear up to the beginning

`ctrl+w`: delete just a word

杀死进程：打开`Activity Monitor`，查看 PID，在终端中：`kill -9 [pid]`

查看当前路径：`pwd`

在当前目录下查找名称：`ls | grep -i python`，`-i`为忽略大小写

在当前目录下根据名称查找文件：`find ./ -name 'default_white.bundle'`

在用户文件夹下查找目录：`find ~ -name 'Autosaved Information' -type d`

列出当前目录下的内容，查找字符串，不区分大小写：`ls | grep -i iOS`

查看文件历史：`git log .//Simeji/SimejiCoreIntl/Resources/keyboardskin/default_white.bundle`

# 清理磁盘空间

`rm -rf ~/Library/Developer/Xcode/iOS\ DeviceSupport`

# Proxy

测试：`curl -i https://google.com`

Set proxy: `export all_proxy=http://10.249.38.21:8128`

Set socks5: `export all_proxy=socks5://127.0.0.1:1086`

# 链接

[Homebrew 进阶命令](https://sspai.com/post/43451)

[Linux 命令五分钟](http://roclinux.cn/?page_id=3759)
