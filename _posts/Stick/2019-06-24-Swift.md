---
title:  "Swift"
categories: [Stick]
---

[Lazy load](https://mikebuss.com/2014/06/22/lazy-initialization-swift/)

[API Design Guidelines](https://swift.org/documentation/api-design-guidelines/) 如何写出清晰、美观的 Swift 风格的接口。Swift 3 之后，这门语言更接近自然语言了。（[参考](https://www.imooc.com/video/13311)）

[apple/swift-evolution](https://github.com/apple/swift-evolution) Swift 的最新发展和用户提议

# Property

Properties associate values with a particular class, structure, or enumeration. 枚举只有Computed properties。

**Stored properties** store constant and variable values as part of an instance.

**Computed properties** do not actually store a value. （并不实际存储值）Instead, they provide a getter and an optional setter to retrieve and set other properties and values indirectly. 并不需要存储这个变量，而是可以通过其他变量计算出来；setter可以间接修改其他变量。

Properties can also be associated with the type itself, known as **type properties**.

If you create an instance of a structure and assign that instance to a constant, you cannot modify the instance’s properties, even if they were declared as variable properties. （常量struct里的变量不能被更改。）

A **lazy stored property** is a property whose initial value is not calculated until the first time it is used. (If a property marked with the lazy modifier is accessed by multiple threads simultaneously and the property has not yet been initialized, there is no guarantee that the property will be initialized only once. 注意lazy变量的多线程访问问题)




# Swift ABI 稳定
<https://onevcat.com/2019/02/swift-abi/>

如何做？安装 Xcode 10.2，然后正常迁移就可以了，不会对你的开发造成什么影响。

什么是ABI稳定？在运行的时候只要是用 Swift 5 (或以上) 的编译器编译出来的 binary，就可以跑在任意的 Swift 5 (Xcode 10.2及以上) 的 runtime 上。这样，我们就不需要像以往那样在 app 里放一个 Swift runtime 了，Apple 会把它弄到 iOS 和 macOS 系统里。因为系统集成了 Swift，所以大家都用同一个 Swift 了，app 启动的时候也就不需要额外加载 Swift，所以在新系统上会更快更省内存。